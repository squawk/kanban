# Session secret for secure cookie encryption (REQUIRED in production)
# Generate with: openssl rand -base64 32
# Must be at least 32 characters
SESSION_SECRET=

# OpenAI API Key for AI prompt generation (optional)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Database URL (SQLite is used by default, MySQL example below)
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# DATABASE_URL=mysql://user:password@localhost:3306/kanban

# Environment (development | production)
NODE_ENV=development
